{
  "name": "markdownpptx",
  "version": "0.0.1",
  "description": "A tool for convert from markdown to pptx",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron src/main.js"
  },
  "author": "spotthediff",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0",
    "electron": "^31.0.2"
  }
}
